
using Warlords.Server.Infrastructure;

namespace Warlords.Server.Hubs
{
    public class GameHub : WarlordsHub
    {
    //    [Authorize]
    //    public Game GetGameStatus(string gameId)
    //    {
    //        Contract.Requires(string.IsNullOrEmpty(gameId) == false, "game id");

    //        var gameStorage = Query<GetCurrentGameStorageQuery>().GetResult();

    //        var result = gameStorage.GetGameById(gameId).Result;
    //        return result;
    //    }
    }
}